# Neon Postgres 연결 문자열 (pooled).
# Vercel Marketplace 로 Neon 을 연결했다면 `vercel env pull .env.local` 로 자동으로 채워진다.
DATABASE_URL=

# OpenAI API 키. 문제·보기·해설 생성에 쓰인다.
OPENAI_API_KEY=

# 선택. 비워두면 gpt-5.4-mini 를 쓴다.
OPENAI_MODEL=
